<%= form_for(@kino) do |f| %>
  <% if @kino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kino.errors.count, "error") %> prohibited this kino from being saved:</h2>

      <ul>
      <% @kino.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label 'nazwa' %>
    <%= f.text_field 'nazwa' %>
  </div>

  <div>
    <%= f.label 'szerokość geograficzna' %>
    <%= f.text_field 'szerokosc' %>
  </div>

  <div>
    <%= f.label 'długość geograficzna' %>
    <%= f.text_field 'dlugosc' %>
  </div>

  <div class="actions">
    <%= f.submit 'Stwórz' %>
  </div>
<% end %>
