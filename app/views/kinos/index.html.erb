<h1>Listing kinos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @kinos.each do |kino| %>
  <tr>
    <td><%= link_to 'Show', kino %></td>
    <td><%= link_to 'Edit', edit_kino_path(kino) %></td>
    <td><%= link_to 'Destroy', kino, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Kino', new_kino_path %>
