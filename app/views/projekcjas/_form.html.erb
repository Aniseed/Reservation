<%= form_for([@kino, @film, @projekcja]) do |f| %>
  <% if @projekcja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projekcja.errors.count, "error") %> prohibited this projekcja from being saved:</h2>

      <ul>
      <% @projekcja.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label 'data' %>
    <%= f.text_field 'data' %>
  </div>

  <div>
    <%= f.label 'godzina' %>
    <%= f.text_field 'godzina' %>
  </div>

  <div>
    <%= f.label 'film:' %>
    <%= @film.tytul %>
  </div>

  <div class="actions">
    <%= f.submit 'Stwórz' %>
  </div>
<% end %>
